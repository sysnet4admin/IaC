apiVersion: v1
kind: Service
metadata:
  annotations:
    lb.kubesphere.io/v1alpha1: porter
    protocol.porter.kubesphere.io/v1alpha1: layer2
  name: porter-svc
spec:
  selector:
    app: nginx
  type:  LoadBalancer
  ports:
    - name: http
      port: 8021
      targetPort: 80