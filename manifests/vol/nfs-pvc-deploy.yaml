apiVersion: apps/v1
kind: Deployment
metadata:
  name: nfs-pvc-deploy
spec:
  replicas: 4
  selector:
    matchLabels:
      app: nfs-pvc-deploy
  template:
    metadata:
      labels:
        app: nfs-pvc-deploy
    spec:
      containers:
      - name: echo-hname
        image: sysnet4admin/echo-hname
        volumeMounts:
        - name: nfs-vol
          mountPath: /var/nfs
      volumes:
      - name: nfs-vol
        persistentVolumeClaim:
          claimName: nfs-pvc