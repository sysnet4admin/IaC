Running kubesprary 
1. login all nodes(master+worker)
2. </b>sh auto_pass.sh</b> from all nodes(master+worker)
3. <b>ansible-playbook kubespray/cluster.yml -i ansible_hosts.ini</b> from m11-k8s only 
   (if you need to add or remove for hosts, please modify ansible_hosts.ini manually.)